<div class="container">
  <h1>Manage All Uebungs</h1>
  <div class="text-center mb-4">
    <%= link_to "Back to Home", root_path, class: "btn btn-primary btn-lg" %>
    <%= link_to 'New Übung', new_admin_uebung_path, class: 'btn btn-primary btn-lg' %>
  </div>

  <div class="table-container">
    <table class="table">
      <thead>
        <tr>
          <th>Title</th>
          <th>Description</th>
          <th colspan="3">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @uebungs.each do |uebung| %>
          <tr>
            <td><%= uebung.title %></td>
            <td><%= truncate(uebung.description, length: 100) %></td>
            <td><%= link_to 'Show', admin_uebung_path(uebung), class: 'btn btn-info' %></td>
            <td><%= link_to 'Edit', edit_admin_uebung_path(uebung), class: 'btn btn-warning' %></td>
            <td><%= link_to 'Destroy', admin_uebung_path(uebung), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
